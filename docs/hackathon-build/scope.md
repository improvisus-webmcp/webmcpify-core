# Scope

Build **WebMCPify Core Agent**, a Strands-powered professional agent that turns an existing web application into reviewed and browser-verified WebMCP capabilities.

The agent must orchestrate Core through MCP, stop for an external human decision before source changes, and report evidence from the real build and browser checks. Amazon Bedrock AgentCore deployment and unrelated product work are out of scope for the deadline.

