# Context Index

This directory is the canonical, source-backed project memory for coding agents and humans.

Use:

```bash
pnpm run barry resume --task "<task>"
pnpm run barry validate
```

## Routes

- `core-platform` — Core CLI, MCP, approval, security, browser verification, repair, and durable workflow architecture.

## Decisions

Architecture decision records live in `docs/context/adrs/`.
